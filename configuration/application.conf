# ------------------------------------------------------------------------------
# Application Runtime Options
# ------------------------------------------------------------------------------

# the default format for a date value 
#dateFormat = "dd.MM.yyyy"

# the name of the system logger
#systemLogger = car.logger.system

# the default format for a timestamp value 
#timestampFormat = "yyyy-MM-dd HH:mm:ss:SSS"

# the folder to hold the configuration files
CONFIG_AREA = configuration

# the folder to hold the bundle workspae
WORKSPACE = workspace
